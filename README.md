aws credentials are stored in ~/.aws/credentials
on mac terraform may be moved to /usr/local/bin/

Within each region, there are multiple isolated datacenters known as Availability Zones (AZs), such as
, , and so on
